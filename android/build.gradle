buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle:8.0.2") // O versión compatible con tu setup
        classpath("com.facebook.react:react-native-gradle-plugin")
        // Añade otras dependencias si las tienes
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
        maven { url("$rootDir/../node_modules/react-native/android") }
    }
}
